# 24/7 hardening revision — file map

## ESP32 files changed

### `main/main.c`
Replace the existing file with this version.

Main hardening:
- bounded STA reconnect backoff in a dedicated task
- safer cJSON request parsing
- safer JSON response generation for configurable strings
- relay persistence only after actual state changes
- three-attempt retry for relay-state NVS writes
- scheduler task watchdog coverage
- existing local-first/captive-portal behavior preserved
- OTA intentionally untouched

### `main/cloud_client.c`
Replace the existing file with this version.

Main hardening:
- 5-second normal cloud polling
- exponential failure backoff up to 60 seconds
- HTTPS-only cloud endpoint
- safer URL joining
- no large per-request heap response allocation
- truncated response detection
- command IDs + explicit acknowledgement queue
- 5-minute command expiry is enforced by the backend
- schedule cache is written to NVS only when the schedule actually changes
- schedule cache migration retained

### `main/cloud_client.h`
No API-breaking change is required; the existing file remains compatible.

## Cloud backend files changed

### `server/src/index.js`
Replace the existing Worker source.

Changes:
- PBKDF2 password records for new passwords
- legacy password record compatibility
- stricter validation
- reliable command acknowledgement
- command expiry
- atomic/batched schedule replacement
- safer device online calculation
- generic server errors instead of leaking internal exception text

### `server/schema.sql`
Replace the existing schema for new deployments.

### `server/migrations/002_reliable_commands.sql`
Apply this migration after `001_schedule_duration.sql` for an existing D1 database.

It adds command expiry/acknowledgement fields and the pending-command index.

### `server/public/index.html`
Replace the existing dashboard.

Small operational fixes:
- expired sessions automatically return to login
- admin can grant device access from the UI
- password creation hint matches the stronger backend requirement
- device token is no longer immediately erased by a dashboard redraw

### `server/README.md`
Updated backend deployment/behavior notes.

## Build/config files

### `.github/workflows/build.yml`
Replace the workflow.

It keeps ESP-IDF v5.1.2 and ESP32 target, adds build concurrency, verifies expected artifacts, and produces SHA-256 checksums.

### `README.md`
Replace the project documentation.

It explicitly states that OTA is deferred and the factory-only partition table is intentionally preserved.

## Files intentionally preserved

- `partitions.csv` — unchanged because OTA is intentionally skipped.
- `sdkconfig.defaults` — unchanged unless a later hardware-specific build review calls for additional tuning.
- `CMakeLists.txt`
- `main/CMakeLists.txt`
- `.gitignore`
- `server/migrations/001_schedule_duration.sql`
- existing firmware binaries are **not part of the source revision** and were deliberately excluded from the final source ZIP.

## Deployment order

### New installation
1. Build the project.
2. Flash the bootloader, partition table, and application generated by the build.
3. Configure the local AP and optional STA/cloud settings.

### Existing ESP32 installation
1. Back up the current NVS/configuration if the device contains important settings.
2. Build this source revision using the same ESP-IDF target/version.
3. Flash the normal factory application using the unchanged `partitions.csv`.
4. Verify local AP control first.
5. Verify physical switches and relay persistence.
6. If cloud is used, deploy the Worker schema/migration changes before testing remote commands.

OTA is deliberately not used for this replacement.

## Production Core v3 additions

### Independent relay controller

`main/relay_backend.c/.h` introduces a backend boundary between smart logic and
physical relay outputs. Direct GPIO remains the default for backward compatibility.
The optional secondary backend uses a separate MCU for physical switch and relay
control.

The secondary-controller source is under
`hardware/secondary_controller_attiny1616/` and uses a CRC16 UART protocol,
watchdog, local state persistence and debounced physical inputs.

### Diagnostics

`GET /api/health` exposes uptime, free/minimum heap, reset reason, connectivity,
and relay-backend health. mDNS exposes `smart-home.local`.

### Cloud command duplicate suppression

Commands already present in the pending ACK list are not executed again while the
ACK is in flight. This makes the command queue idempotent from the device side.
